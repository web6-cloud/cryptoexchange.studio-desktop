import { Layout as h } from './horizontal'
import { Layout as p } from './picto'
import { Layout as s } from './square'

export const layouts: Logo.Layouts = {
  horizontal: h,
  square: s,
  picto: p,
}
